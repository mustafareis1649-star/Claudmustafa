// Runs entirely in the browser — no file/text is ever uploaded to a server.
// TODO: replace this with the tool's real logic. Keep it plain JS (no React)
// so it stays easy to unit test on its own, same as logic/pdfToWord.js.

/**
 * @param {string} input
 * @returns {any}
 */
export function runWordCounter(input) {
  return input;
}
